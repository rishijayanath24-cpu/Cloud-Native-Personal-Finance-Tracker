apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: budget-postgres-pvc
  namespace: finance-tracker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: managed-csi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-budget
  namespace: finance-tracker
  labels:
    app: postgres-budget
spec:
  serviceName: postgres-budget
  replicas: 1
  selector:
    matchLabels:
      app: postgres-budget
  template:
    metadata:
      labels:
        app: postgres-budget
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: budgetdb
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "financeuser", "-d", "budgetdb"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "financeuser", "-d", "budgetdb"]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: budget-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-budget
  namespace: finance-tracker
spec:
  selector:
    app: postgres-budget
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
